# 本地视频播放器 App — 云端构建说明

## 当前状况
- 应用源码已完整写好（`lib/` 下 4 个文件 + `pubspec.yaml` + Android 工程），在**普通环境（带 Flutter SDK 的电脑 / 云端 CI）可以正常构建出 APK**。
- 当前这个 WorkBuddy 沙箱环境**无法构建**：它在构建重负载阶段会掐杀 Gradle 进程，且 Gradle 9 在 Windows Server 2022 上加载原生库（`native-platform.dll`）失败。连 8/9 能成功构建的同类工程现在也构建不了。这是环境限制，不是代码问题。

## 怎么拿到可安装的 APK（推荐：GitHub Actions 云端免费构建）

你不需要在自己的电脑上装任何开发工具，全程用网页/手机即可：

1. 注册一个 GitHub 账号（免费）：https://github.com
2. 新建一个**公开**仓库（名字随便，例如 `local-video-player`）。
3. 把本目录（`local_video_player` 项目，已包含 `.github/workflows/build-apk.yml`）推送到该仓库：
   - 简单方式：用 GitHub 网页或手机 App 直接把项目文件上传到仓库；
   - 或把仓库地址/Token 给 AI，由 AI 帮你推上去。
4. 推送后，GitHub 会自动触发一次 **Build Android APK** 构建（仓库里已经放好了构建脚本）。
5. 进入仓库的 **Actions** 页面 → 等构建变绿（约 3–6 分钟）→ 在产物（Artifact）里下载 **app-debug-apk**（即 `app-debug.apk`）。
6. 把 APK 传到手机安装即可测试（安卓需在「设置 → 安全」里允许「未知来源」安装）。

> 说明：这里产出的是 **debug APK**，功能与正式版完全一致，可直接安装试用。后续若要做正式发布版（release + 签名），再补一个签名配置即可。

## 备选方案 A：Codemagic（Flutter 专用 CI，更省心）
- 注册 https://codemagic.io（免费额度够用）。
- 用 GitHub 账号登录并授权你的仓库。
- Codemagic 能自动识别 Flutter 项目，无需理解配置文件，点一下就构建并给出可下载的 APK。

## 备选方案 B：你自己有装了 Flutter 的电脑
在项目目录执行两条命令即可：
```
flutter pub get
flutter build apk --debug
```
生成的 APK 在 `build/app/outputs/flutter-apk/app-debug.apk`。

## 如果你希望 AI 直接帮你推到 GitHub
把你的 GitHub 仓库地址和 Personal Access Token（需 `repo` 权限）发给我，我可以在当前环境执行 `git` 推送，并确认 Actions 构建成功、APK 产出。
